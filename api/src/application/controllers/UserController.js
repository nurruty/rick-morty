const getUser = async () => {
  const user = {
    email: 'nico',
    id: 1,
  };

  return user;
};

const loginUser = async (data) => {
  const user = {
    email: 'nico',
    id: 1,
  };

  return user;
};

module.exports = {
  getUser,
  loginUser,
};
