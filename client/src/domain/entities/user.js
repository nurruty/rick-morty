// const createUser = ({ userName, email } = {}) => ({
//   userName,
//   email,
//   isLoggedIn: !!email,
// });

const createUser = () => ({
  userName: 'Nico',
  email: 'nicourruty@gmail.com',
  isLoggedIn: true,
});

export { createUser };
